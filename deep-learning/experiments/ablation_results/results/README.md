# Results

Shared output artifacts across components.

```
results/
├── figures/   # Publication / report figures
├── reports/   # Generated reports, tables, summaries
└── logs/      # Run logs
```

Contents are git-ignored by default (keep the structure, not the outputs).
